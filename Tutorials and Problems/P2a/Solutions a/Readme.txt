Four stages in the first exercise on the Lunch List Application

1. The first interface: only LinearLayouts with disaligned EditText boxes

2. The second interface with a TableLayout (column alignment) and a RadioGroup

3. Adding a simple ListView

4. A more elaborate ListView and Tabs on the same Activity